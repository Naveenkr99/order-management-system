spring:
  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:1111}
  application:
    name: order-service

application:
  config:
    customer-url: localhost:8222/api/v1/customers
    payment-url: localhost:8222/api/v1/payments
    product-url: localhost:8222/api/v1/products
